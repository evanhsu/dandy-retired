source 'http://rubygems.org'

gem 'rails', '3.0.3'

# Use older version of Rake
#gem 'rake', '0.9.2.2'

# Bundle with the MySQL adapter (the 'mysql2' gem caused incorrect datatype errors when retrieving data)
gem 'mysql'
#gem 'mysql', '2.8.1'
gem 'mysql2', :group => :production
gem 'activerecord-mysql2-adapter', :group => :production

gem 'dynamic_form'
gem 'prawn', '0.12.0'
#gem 'rack', '1.2.1'
gem 'jquery-rails'
gem 'yaml_db', :git => "https://github.com/blanchma/yaml_db.git"
gem 'whenever', '0.7.3' # For assisting with CRON scheduling

# Use unicorn as the web server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger (ruby-debug for Ruby 1.8.7+, ruby-debug19 for Ruby 1.9.2+)
# gem 'ruby-debug'
# gem 'ruby-debug19'

# Bundle the extra gems:
# gem 'bj'
# gem 'nokogiri'
# gem 'sqlite3-ruby', :require => 'sqlite3'
# gem 'aws-s3', :require => 'aws/s3'

# Bundle gems for the local environment. Make sure to
# put test-only gems in this group so their generators
# and rake tasks are available in development mode:
# group :development, :test do
#   gem 'webrat'
# end
